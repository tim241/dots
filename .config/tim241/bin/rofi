#!/usr/bin/env bash
set -e
config_dir="${XDG_CONFIG_HOME:-$HOME/.config}"

theme="$("$config_dir/tim241/bin/theme" get)"

rasi_file="$config_dir/tim241/themes/$theme/rofi.rasi"

rofi -theme "$rasi_file" -width 100 -location 3 $@
